# plan.md — 甲子園トーナメント予想アプリ（koshien-yosou）

## 要件

- 夏の甲子園（2026年・第108回を初回ターゲット）の各試合の勝敗を予想し、的中ポイントでランキングを競う。
- **金銭・賞品のやり取りは一切なし**（賭博に該当しない娯楽アプリ）。
- まずはローカルプロトタイプ。将来、友人内Webアプリへ拡張できる構造にはするが、認証・デプロイは今回スコープ外。
- 試合結果は外部サイトから自動取得（後述の規約確認を条件とし、手動入力フォールバックを必ず持つ）。

## 前提・制約

- 夏の甲子園は組み合わせが段階的に決まる（3回戦以降は再抽選）ため、全試合一括のブラケット予想は不可能。
  → **「試合ごとの勝敗予想」方式**を採用。組み合わせが確定した試合から順次予想可能になる。
- 大会日程: 組み合わせ抽選 8月上旬 → 開幕 8月上旬 → 決勝 8月下旬。開発はダミーデータ（過去大会の組み合わせ）で先行し、本大会データは抽選後に投入。
- ローカル利用なのでログインなし。ニックネーム選択式で複数人（家族・友人が同じPCで）参加可能。

## ポイントルール（v1）

| ラウンド | 基礎ポイント |
|---|---|
| 1回戦 | 10 |
| 2回戦 | 15 |
| 3回戦 | 20 |
| 準々決勝 | 30 |
| 準決勝 | 40 |
| 決勝 | 50 |

- 予想締切: 各試合の開始予定時刻（未設定の試合は試合日の 8:00）。締切後は変更不可・未提出は0点。
- 優勝校予想: 開幕前にロック。的中で +100pt。
- 同点は同順位。

## オッズ（配点倍率）

的中ポイント = 基礎ポイント × 倍率。倍率は**参加者の予想分布から自動算出**する（パリミュチュエル方式）。

- 計算式: `倍率 = clamp(その試合の予想提出者数 ÷ 勝利チームに張った人数, 1.0, 3.0)`
  - 全員が本命に張って的中 → 1.0倍。半々に割れて的中 → 2.0倍。少数派の的中 → 上限3.0倍。
  - 上限3.0倍は「大穴1発でランキング決定」を防ぐため。下限1.0倍で基礎ポイントは保証。
- 倍率の確定は**締切時点の分布**で行う。小数第2位で四捨五入、ポイントは整数に四捨五入。
- **オッズは常時表示（変動オッズ方式）**: 競馬の単勝オッズと同様、締切まで分布に応じて変動する暫定オッズを画面に出し、締切時点の分布で確定する（2026-07-24 ユーザー要望: ブックメーカー風UIに変更。締切まで非公開の当初案を廃止）。予想人数の内訳は締切後のみ公開。
- **長期（優勝予想）もオッズ化**: 優勝ボーナス = `100pt × clamp(優勝予想の提出者数 ÷ その校に張った人数, 1.0, 5.0)`。開幕（最初の試合の締切）でロック・確定。
- UIはブックメーカー風のダーク画面（試合リスト＋各チームのオッズチップ、「長期」タブに優勝オッズ一覧）。あくまで見た目の雰囲気のみで、金銭・入出金・賞品要素は一切実装しない。
- ガードレール: オッズはチームの勝敗のみに付ける。**未成年個人を対象にした予想項目（本塁打・奪三振など個人成績）は実装しない**。金銭・賞品との交換機能も実装しない。

## 技術構成（最小構成）

- Node.js + Express + better-sqlite3。フロントは素の HTML/JS/CSS（ビルド無し）。
- ファイル構成:
  - `server.js` — API・静的配信・DB初期化
  - `fetcher.js` — 結果自動取得アダプタ（実行は管理画面の「結果更新」ボタンから。常駐ポーリングはしない）
  - `public/index.html` / `public/app.js` / `public/style.css` — 予想・ランキング・管理タブ
  - `koshien.db` — SQLite（git管理外）

## データモデル

- `users(id, name)`
- `teams(id, name, prefecture)` — 49代表校
- `matches(id, round, day, game_no, team1_id, team2_id, winner_id, score1, score2, scheduled_at, status)`
- `predictions(user_id, match_id, predicted_winner_id, created_at)` — UNIQUE(user_id, match_id)
- `champion_picks(user_id, team_id, locked_at)`

ポイントは predictions と matches から都度計算（集計テーブルは作らない）。

## 結果自動取得の方針

1. 候補ソース: 朝日新聞「バーチャル高校野球」、NHK、スポーツナビ。
2. **実装前に robots.txt と利用規約を確認**し、自動取得が許容されるソースを選ぶ。取得は「結果更新」ボタン押下時のみ・1回につき数リクエスト・適切な間隔の礼儀的アクセスに限定（ローカル個人利用）。
3. 許容されるソースが無い場合は自動取得を断念し、管理タブからの手動入力（1日最大4試合なので数十秒の作業）に切り替える。
4. いずれの場合も手動入力UIは実装する（自動取得の失敗時フォールバック兼用）。

## 大会（トーナメント）対応（2026-07-24 追加要望）

- `tournaments` テーブルを導入し、**甲子園と地方大会を大会単位で管理**する。画面上部のセレクタで切替。
- ラウンド構成・基礎ptは大会種別テンプレートで持つ:
  - 甲子園: 1回戦10 〜 決勝50（従来どおり）、優勝ボーナス100pt
  - 地方大会: 1回戦5 〜 決勝20（試合数が多く1試合の重みが軽いため低配点）、優勝ボーナス50pt
- 優勝予想は**大会ごと**に1つ（地方大会の優勝予想＝甲子園出場校の予想）。ロックはその大会の最初の試合の締切。
- 地方大会の出場校は管理タブから追加（チーム追加フォーム）。大会・試合も管理タブから追加。

## 持ち点制・ステーク選択（2026-07-24 追加要望「ベット数も増やしていい」→ 同日簡素化）

- 全ユーザー初期持ち点 **1000pt**。ベット時にステークを差し引き、的中で `round(ステーク × オッズ)` を獲得。
- **試合ベットは1タップ +10pt・1試合最大100pt**（チップ選択UIは廃止。同じ学校を連打で増額、別の学校タップで10ptから張り直し、締切前は取消も可＝全額返却）。
- **オッズはポイント量加重のパリミュチュエル**: `clamp(総プール ÷ その側のプール, 1.0, 3.0)`。
- **優勝ベットは1ベット原則100pt固定**。払戻は `100pt × オッズ（人数比、上限5.0）`。
  - **レイト参加費**: 参加コスト = `100 + 100 × 消化済みラウンド数`。増額分は払戻に含まれない「後出し料」（後から張るほど実質オッズが下がる＝情報アドバンテージの相殺）。締切は決勝開始まで。
- 持ち点が10pt未満になったら**救済チップ +100pt** を取得可能。取得回数はランキングに表示（乱発の社会的抑止）。
- ランキングは**確定収支**（決着済みベットの `獲得 − ステーク` の合計）で順位付け。持ち点・的中数・救済回数も表示。
- 持ち点は残高カラムを持たず**ベット履歴から毎回導出**する（初期値 − 全ステーク + 全払戻 + 救済×100）。結果の修正時に払戻の巻き戻しが自動で整合するため。

## 本戦ルール（2026-07-28 追加要望・甲子園開幕前に適用）

- **パスワード認証**: 参加は名前＋パスワード（4文字以上、scrypt ハッシュ保存）。ベット系APIはログイントークン必須で**自分のベットは自分だけが操作可能**。パスワード導入前の既存ユーザーは初回ログイン時のパスワードがそのまま設定される。
- **管理者**: `config.json`（git管理外）の `admin_password` でログイン。管理API（結果入力・試合/大会/学校追加・AI予想・不成立）に加え、`user_id` 指定で**任意ユーザーの代理ベット・修正が可能**。
- **ベット上限撤廃**: 試合ベットは下限10ptのみ・上限なし（持ち点の範囲内）。UIはタップ増分（+10/+50/+100/+500）を選んで連打する方式。
- **不成立（不戦勝・中止）**: 管理の「不成立」でその試合の全ベットを**全額返還**。
- **延長ルール**: 9回で決着しない場合は**同点扱いでベット没収**。ただし**延長で勝ったチームに張っていた人だけ半金返還**（オッズ配当なし）。結果入力の「延長決着」チェックで適用。

## AI予想・全国横断リスト（2026-07-24 追加要望）

- **AI予想**: `ai_predictions` テーブル（試合ごとに予想校・優勢度50〜95%・コメント）。Claude が試合情報（直近の結果・戦力情報）を分析して `/api/admin/ai` で投入する。**表示専用でプールには参加しない**（オッズを歪めないため）。
- **試合予想タブは全大会横断**: 今日以降の試合を日付順に全部表示（「昨日以前も表示」トグルあり）。ブロック（北海道・東北／関東／…）と都道府県で絞り込み。大会セレクタは長期（優勝）タブ用。

## ライブオッズ配信（2026-07-24 追加要望）

- ベット（予想）が入った瞬間にオッズを全クライアントへ反映する。実装は **SSE（Server-Sent Events）**: 予想・優勝予想・結果確定・試合追加の各POST後にブロードキャスト → クライアントが再取得。
- オッズが動いたチームには **▲（上昇）/▼（下降）** を表示。フォールバックとして60秒ポーリングも維持。

## マイルストーン

1. **M1 スケルトン**: DB・API・画面の骨組み + ダミーデータ（過去大会ベースの49校と1回戦組み合わせ）
2. **M2 コア機能**: 予想提出（締切制御）→ 結果確定 → ポイント計算 → ランキング表示、優勝校予想
3. **M3 自動取得**: 規約確認 → fetcher 実装 or 手動入力運用に確定
4. **M4 本番投入**: 組み合わせ抽選後に2026年の実データ投入、動作確認

## スコープ外（将来）

- 友人内Web公開（認証・ホスティング）、スコア差予想などの追加配点、春のセンバツ対応。
